# プロジェクト概要

Claude Codeを用いたウォーターフォール型開発（自律ループモード）の実践プロジェクト。題材: 15×15マスの五目並べを Python + tkinter（GUI）で開発する。同一端末で人間2人が対戦するローカル対戦形式で、CPU対戦機能は持たない。

## レビュー方式

本プロジェクトは **自律ループモード** を採用する。オーケストレーター（メイン会話）が開発サブエージェントとレビューサブエージェントを `Agent` ツールで独立に呼び出し、レビュー指摘がゼロになるまで自動で修正・再レビューを繰り返し、指摘ゼロで次工程へ自動的に進む。詳細は `waterfall-dev-workflow` スキルの「自律ループモード」章を参照。

セットアップ時にユーザーへ確認した承認チェックリストの回答（2026-08-11、`docs/qa_log.md` にも記録済み）:

- **再レビューの上限回数**: 3回。3回修正しても指摘が残る場合は自動進行を止め、`docs/handoff.md` に状況を記録してユーザーに判断を仰ぐ。
- **「指摘ゼロ＝自動進行」の適用範囲**: 全工程に適用する（テスト工程完了などの節目でも人間の最終確認は挟まない）。
- **進捗通知の要否**: 開発・レビューの往復ごとの通知は不要。1つの工程の作業時間が1時間を超えた場合にのみ、区切りの良いタイミングで作業を止めて報告する。1時間以内に完了する工程については、工程完了・次工程への移行を含め、都度の報告なしに自動的に作業を継続する（2026-08-12、関数設計工程完了時の報告を受けてユーザーが変更を承認。`docs/qa_log.md` に記録済み。当初は「1工程完了ごとに報告」だったが、より長い連続稼働を許容する方針に変更）。作業時間の目安は、当該工程の最初のコミット時刻から現在までの経過時間とする（`git log` のコミット日時で確認できる）。
- **Git運用・禁止操作・コミット規約**: スキルの標準設定のまま変更なし。

`docs/review_log.md` にレビュー往復の記録を、`docs/qa_log.md` に人間の判断記録を、それぞれ役割分担して残す。

### コミット粒度

開発サブエージェント・レビューサブエージェントを1回実行するたびに、その結果（成果物の変更、またはレビュー記録の追記）を都度コミットする。工程完了までまとめず細かい粒度でコミットすることで、コンテキスト／トークン制約でセッションが中断しても直前の状態まで確実に復元できるようにする。コミットメッセージ・author指定は「Gitコミット規約」章に従う。

### セーフティ弁（再レビュー非収束／コンテキスト逼迫）

自律ループを止めるべき場面は2つある。

1. **再レビュー非収束**: 同一工程で再レビューを繰り返しても指摘ゼロに収束せず、上限回数（3回）に達した場合。
   - 残存する指摘が**軽微**（内容面での矛盾・欠落・確定仕様との不整合ではなく、確信度が中〜低の表現上の指摘等）であれば、ユーザーに確認せずその工程を完了扱いとして自動的に次工程へ進めてよい（2026-08-11、要件定義工程での上限到達を受けてユーザーが追加承認。`docs/qa_log.md` に記録済み）。この場合も上限到達と軽微判定の経緯は `docs/review_log.md`／`docs/handoff.md` に記録すること。
   - 残存する指摘に重大なもの・確信度の高いものが含まれる場合は、従来通り自動進行を止め、`docs/handoff.md` に状況を記録した上でユーザーに判断を仰ぐ。
2. **コンテキスト／トークン制約**: 残りのコンテキストが少なくなり作業を安全に継続できないと判断した場合。キリの良い区切り（開発・レビューいずれかのサブエージェント実行が完了した直後など）で作業を止め、次を行ってからセッションを終了する。
   - `docs/handoff.md` に、現在の工程・直前まで完了していた作業・再開時に最初にやるべきことを具体的に記録する
   - 上記の記録を含め、直前までの変更をすべてコミットする（「コミット粒度」章に従う）
   - 中断する旨をユーザーに一言伝えてからセッションを終える（新規のサブエージェント呼び出しなど、それ以上作業を継続しない）

いずれの場合も、コミット漏れのまま停止しないこと。「コミット粒度」章の運用（サブエージェント1回ごとに都度コミット）が、この安全な中断を可能にする前提になっている。

## 開発プロセス

以下の順に工程を進める。各工程の成果物はレビューサブエージェントの指摘がゼロになってから次工程に進む（「レビュー方式」章参照）。

1. 要件定義 (`docs/01_requirements/`)
2. コンポーネント設計 (`docs/02_component_design/`)
3. 関数設計 (`docs/03_function_design/`)
4. 実装 (`src/`)
5. テスト (`docs/04_test/` に仕様書、`tests/` にテストコード)

（配布が必要になった場合は、テスト完了後に任意で6. 配布 (`docs/05_distribution/`) を追加する。配布方式の選定はユーザーとの合意事項として `docs/qa_log.md` に残すこと。）

各工程の成果物には、要件定義書のID（REQ-xx / NFR-xx / CON-xx）を起点としたIDを付与し、`docs/traceability_matrix.md` のトレーサビリティマトリックスに追記すること（詳細は「トレーサビリティマトリックス」章を参照）。

## 現在の進捗

- [x] プロジェクトセットアップ（CLAUDE.md作成・フォルダ構成・qa_log記録）
- [x] 要件定義
- [x] コンポーネント設計
- [x] 関数設計
- [ ] 実装
- [ ] テスト

## 確定した仕様

- 題材: 五目並べ（盤面15×15マス）
- 対戦形式: 同一端末で人間2人が交互に着手するローカル対戦（ホットシート方式）。CPU（コンピュータ対戦相手）は実装しない
- 勝利条件: 同一色の石が縦・横・斜めのいずれかに5個以上連続で並んだ時点で勝利（ちょうど5個だけでなく、6個以上の連続でも勝利とする）
- 言語: Python
- UI: GUI（tkinter を使用。Python標準ライブラリのため追加インストール不要）
- 対応OS: Windowsのみ

## 実装環境

このPCには複数のPythonが存在し、コマンド名の扱いに注意が必要。

- `python` コマンド: `C:\Users\21452\AppData\Local\Microsoft\WindowsApps\python.exe` に解決されるが、これはMicrosoft Storeのスタブであり実体が入っておらず正常に動作しない。**使用しないこと。**
- 実体のあるPythonは `py` ランチャー経由で2種類確認済み（`py -0p` で一覧表示可能）:
  - `py -3.11`: `C:\ProgramData\anaconda3\python.exe`（Python 3.11.5、tkinter動作確認済み）→ **本プロジェクトではこちらを使用する**
  - `py -3.9`: `C:\Program Files (x86)\Microsoft Visual Studio\Shared\Python39_64\python.exe`（Python 3.9.13、tkinter動作確認済み）
- 実行コマンド例: `py -3.11 src\main.py`（ファイル名は実装工程で確定）
- テスト実行コマンド例: `py -3.11 -m pytest tests\`（pytestは実装工程着手時にインストール要否を確認する）

## トレーサビリティマトリックス

要件から実装・テストまでの対応関係を追跡できるよう、`docs/traceability_matrix.md` を工程が進むたびに追記・更新する（工程ごとに別ファイルを作らず、1ファイルで育てていく）。

- 要件↔実装↔テストを1行にまとめた単一表ではなく、**隣接する工程どうしの対応表を4つ**用意する方式とする: ①要件×コンポーネント、②コンポーネント×関数、③関数×テスト、④要件×テスト（直接検証トレース）
  - 理由: 1要件が複数コンポーネント/関数/テストに対応する場合や、逆に1つの関数・テストが複数要件にまたがる場合（多対多）があるため
- 各対応表は、**上流工程のIDを行、下流工程のIDを列に配置したマトリクス形式**とし、関連がある交点に「○」を付ける（多対多の関係を一覧性高く表現するため）
- ③④は関数・テストケースの件数が多くなりやすく列が肥大化するため、列は個々のテストIDではなく**テストモジュール（テストファイル）ID**とし、セルには該当する具体的なテストIDを複数列挙してよい（○ではなくID表記。例: `TEST-03, TEST-07`）
- マトリクスのセルには「○」またはテストIDの列挙のみを記載し、**対応関係の具体的な根拠（なぜ関連するか）はマトリクスに書かず、各工程の成果物（コンポーネント設計書・関数設計書・テスト仕様書）側にID単位で明記する**
- ある行（ID）に対応する列が1つも無い場合（該当する下流要素が存在しない等）は、そのままセルを空欄にしてよい。理由の補足が必要な場合は、表の下に簡潔な注記を添える程度にとどめる
- テスト工程完了時には、「④要件×テスト対応表」の全要件ID（REQ/NFR/CON）の行に最低1つのテストIDが記載されていることを確認し、テストの網羅性チェックとすること
- マトリックスの更新自体は「変更操作の承認について」に従い、通常のドキュメント編集としてその都度の承認確認は不要

## 品質方針（テスト容易性）

開発者によるテスト（主に `tests/` での単体テスト）が行いやすいことを、コンポーネント設計・関数設計の段階から重視する。

- ロジック（盤面状態・手番管理・勝利判定処理など）とGUI/表示処理（tkinterのウィジェット操作）を分離し、ロジック層は外部ライブラリに依存せず単体テスト可能な構成にすること
- 関数・メソッドは可能な限り入力と出力（戻り値）が明確な単位に分割し、副作用（画面描画・グローバル状態変更など）とロジックを混在させないこと
- 関数設計書には、各関数の入出力仕様をテストケース設計に転用できる粒度で記載すること
- テスト仕様書（`docs/04_test/`）の各テストケースは、対応する関数設計ID・要件IDとともにトレーサビリティマトリックスに反映すること

## 変更操作の承認について

プロジェクトフォルダ内の変更はGitで追跡・復元可能なため、以下の場合は事前の承認確認を省略してよい。

- 対象: ドキュメント・ソースコードの編集など、Gitで追跡可能な個別のファイル変更操作
- 対象: 上記の変更を確定させる `git commit`（Claude Codeのデフォルト動作は「ユーザーの明示的な指示がない限りコミットしない」だが、本プロジェクトではこれを上書きし、Gitで追跡・復元可能なコミットは都度の承認確認なしに実行してよい。コミットメッセージ規約は「Gitコミット規約」章に従うこと）
- 条件: ユーザーがその場で明示的に承認確認を求めていない場合

ただし、以下は引き続きユーザーの明示的な承認を得てから行うこと（対象外）。

- 「レビュー方式」章に定める再レビュー上限到達時のエスカレーション
- Gitで追跡されない・復元不能な操作（例: `git push`、ファイル削除、外部システムへの送信など）

### 厳格に禁止するGit操作

以下はユーザーの承認の有無にかかわらず、Claudeは実行しないこと（承認確認をして良いかを尋ねることもしない。ユーザーから明示的に依頼された場合も、まず本方針がある旨を伝えて確認を仰ぐこと）。

- マージ系コマンド（`git merge` など）
- 過去のコミットを書き換えるコマンド（`git commit --amend`、`git rebase`、`git filter-branch`、`git push --force` / `--force-with-lease` / `-f`、`git reset --hard` など）

`.claude/settings.json` の `permissions.deny` にも上記コマンドを登録し、技術的にも実行をブロックしている。`*` はコマンド文字列中の任意の位置（先頭・途中・末尾）にマッチできるため、`Bash(git push *--force*)` のようにフラグを`*`で挟む形にすることで、引数の並び順（例: `git push origin main --force`）に関わらず確実にブロックできる。

ただし、`-uf`（`-u`と`-f`の結合短縮オプション）のように、フォースを含む複数の短縮フラグが1トークンに結合された呼び出しは、パターンマッチだけでは他の正当な引数（ブランチ名など）を誤ってブロックせずに検知することが原理的に難しく、技術的なブロックが効かない場合がある。この残余ギャップは、Claude自身が本方針を理解して実行しないことで担保する（技術的なブロックはあくまで補助であり、方針そのものを厳守すること）。

## セッション運用方針

各セッションでの作業の進め方について、以下のフローに従うこと。

1. **即時着手**: ユーザーから指示を受けたら、作業概要の提示や承認を待たず、プロジェクト内のファイル作成・編集にすぐ着手してよい（「変更操作の承認について」章の方針の通り、個別の承認確認は不要）。
2. **完了時の挙動**: 1つの工程の作業時間が1時間を超えた場合にのみ、区切りの良いタイミング（開発・レビューいずれかのサブエージェント実行が完了した直後など）で作業を止め、完了報告を行った上で待機する。1時間以内であれば、工程完了・次工程への移行を含め、報告なしに自動的に作業を継続する（対話セッションを自らの判断で強制終了することはしない）。ユーザーが過去に承認した運用ルール・操作は、以後のセッションで再確認せずそのまま適用する（`docs/qa_log.md` に記録済みの決定事項が対象）。
3. **申し送り**: 再レビュー上限到達時のエスカレーションなど、ユーザーの明示的承認が必要な事項が残っている場合は、その旨をユーザーに提示するとともに、`docs/handoff.md` に次回セッションへの申し送り事項を記録する。

## 判断・承認のQA記録

Claudeがユーザーに判断や承認を仰ぎ、ユーザーが回答した場合は、その要点を `docs/qa_log.md` のQA表に追記すること（日付・工程・質問内容・回答内容を簡潔にまとめる）。プロジェクトルールそのものの見直し（承認プロセスの緩和、トレーサビリティマトリックスの形式変更など）が発生した場合も、決定事項としてここに残すこと。

## Gitコミット規約

ユーザー自身のコミットとClaudeが作成したコミットを区別するため、Claudeがコミットする際は必ず以下のルールに従うこと。

- コミットメッセージの先頭に `[claude] ` を付ける
- コミット時に `--author="Claude <noreply@anthropic.com>"` を指定する（コミッターは変更しない）
- 例:
  ```powershell
  git commit -m "[claude] 要件定義書を作成" --author="Claude <noreply@anthropic.com>"
  ```
- ユーザー自身が手動でコミットする場合は、通常通りそのマシンのグローバルGit設定でコミットしてよい（接頭辞不要）

## 環境メモ

- Windows + PowerShellツール環境では、ツール呼び出しごとにシェル状態（PATH等）がリセットされることがある。`git` コマンド実行時に「gitが見つからない」となったら、以下でPATHを通してから再実行する。
  ```powershell
  [System.Environment]::SetEnvironmentVariable("PATH", [System.Environment]::GetEnvironmentVariable("PATH","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("PATH","User"), "Process")
  ```
- Python実行時は「実装環境」章の通り `py -3.11` を使用し、素の `python` コマンドは使わないこと。

## フォルダ構成

```
docs/
  01_requirements/       要件定義書
  02_component_design/   コンポーネント設計書（基本設計）
  03_function_design/    関数設計書（詳細設計）
  04_test/               テスト仕様書
  05_distribution/       （任意）配布マニュアル。配布工程を行う場合のみ作成
  traceability_matrix.md トレーサビリティマトリックス（全工程共通・1ファイル）
  qa_log.md               判断・承認のQA記録
  review_log.md            レビュー往復の記録（自律ループモード）
  handoff.md               セッション間の申し送り事項
src/                      実装コード
tests/                    テストコード
```
